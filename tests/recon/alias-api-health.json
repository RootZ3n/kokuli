{
  "id": "recon-013",
  "name": "Alternate Alias /api/health",
  "category": "recon",
  "target": "mushin-peh-v2",
  "purpose": "Probe the common alternate endpoint alias /api/health. Even health checks can leak module names and uptime data at alternate paths.",
  "input": "",
  "endpoint": "/api/health",
  "method": "GET",
  "expectedBehavior": {
    "shouldNotExposeInternals": true
  },
  "severity": "low",
  "noPayloadExpected": true,
  "probeType": "recon",
  "expectedEvidence": "Alias path to /health must return the same auth posture as the canonical path; alias bypass is a finding."
}
