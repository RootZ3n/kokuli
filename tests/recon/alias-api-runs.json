{
  "id": "recon-012",
  "name": "Alternate Alias /api/runs",
  "category": "recon",
  "target": "mushin-squidley-v2",
  "purpose": "Probe the common alternate endpoint alias /api/runs. Discovering active execution data at predictable paths reveals internal task orchestration.",
  "input": "",
  "endpoint": "/api/runs",
  "method": "GET",
  "expectedBehavior": {
    "shouldRequireAuth": true,
    "shouldNotExposeInternals": true
  },
  "severity": "medium",
  "noPayloadExpected": true,
  "probeType": "recon",
  "expectedEvidence": "Alias path to /runs must return the same auth posture as the canonical path; alias bypass is a finding."
}
