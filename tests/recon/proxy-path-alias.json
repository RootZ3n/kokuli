{
  "id": "recon-017",
  "name": "Proxy Path Alias",
  "category": "recon",
  "target": "mushin-peh-v2",
  "purpose": "Probe double-slash and dot-segment path aliases that some reverse proxies normalize differently, potentially exposing backend routes.",
  "input": "",
  "endpoint": "//health",
  "method": "GET",
  "expectedBehavior": {
    "shouldErrorSafely": true,
    "shouldNotExposeInternals": true
  },
  "severity": "medium",
  "noPayloadExpected": true,
  "probeType": "recon",
  "expectedEvidence": "Proxy-prefixed alias must return the same auth/visibility posture as the canonical path."
}
